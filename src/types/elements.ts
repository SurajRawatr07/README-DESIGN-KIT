// Base
import type { ViewMode } from '@/types/view-mode';

export interface BaseElement {
  id: s

lement
  | ImageElement
  | CodeBlockElement
  | TableElement
  | BadgeElement
  | DividerElement
  
